# Learning Center Video Placeholders

This folder is reserved for Learning Center module videos.

Suggested structure:

- `public/videos/learning/<section>/<module-id>/video.mp4`

The current Learning Center UI renders videos as placeholders (it does not load these files yet),
but keeping a stable folder structure makes it easy to wire in real videos later.
